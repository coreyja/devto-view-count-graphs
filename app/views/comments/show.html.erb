<%= render 'layouts/graph_page', presenter: @presenter, active: :comments do %>
  <%= line_chart(
    @presenter.article_stats.group_by_hour(:recorded_at).maximum(:comments_count),
    xtitle: @presenter.timespan_in_words,
    ytitle: 'Comments',
  ) %>
<% end %>
