<h1 class="text-4xl text-center"><%= @article.title %></h1>
<% if @article.cover_image %>
  <%= image_tag @article.cover_image %>
<% end %>
<div id="spacer" class="flex-grow"></div>
<%= line_chart @article.article_stats.group_by_hour(:recorded_at).maximum(:comments_count), width: '100%' %>
<div id="spacer" class="flex-grow"></div>

<%= render 'graph_nav_footer', active: :comments, article: @article %>
