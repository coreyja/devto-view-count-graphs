<%= render 'layouts/graph_page', presenter: @presenter, active: :views do %>
  <%= line_chart(
    @presenter.article_stats.group_by_hour(:recorded_at).maximum(:page_views_count),
    xtitle: @presenter.timespan_in_words,
    ytitle: 'Views',
  ) %>
<% end %>
