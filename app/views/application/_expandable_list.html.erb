<div data-controller="expandable-list" data-expandable-list-expanded-value="false">
  <h1 class="inline-block p-4 font-serif text-xl bg-blue-200 rounded-full cursor-pointer" data-action="click->expandable-list#toggle">
    <%= locals[:title] %>
    <span class="inline-block transform" data-expandable-list-target="icon">
      >
    </span>
  </h1>
  <ul data-expandable-list-target="list">
    <%= yield %>
    <% @articles_this_month.each do |article| %>
    <% end %>
  </ul>
</div>
